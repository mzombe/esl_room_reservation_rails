<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <%= link_to 'Home', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Reservation
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to 'New Reservation', new_reservation_path, class:"dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to 'View Reservation', reservations_path, class:"dropdown-item" %></li>
          </ul>
        </li>
      </ul>
      <form class="d-flex">
        <% if user_signed_in? %>
            <%= link_to current_user.email, edit_user_registration_path, class:"nav-link pe-1" %> 
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link ps-1" %> 
        <% else %>
            <%= link_to 'Sign Up', new_user_registration_path, class:"nav-link pe-1" %> 
            <%= link_to 'Sign In', new_user_session_path, class:"nav-link ps-1" %>
        <% end %>
      </form>
    </div>
  </div>
</nav>